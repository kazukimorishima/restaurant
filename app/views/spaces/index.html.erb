<div class="admin">
    <h1>スペース写真一覧</h1>
    <hr>
    <% @spaces.each do |s| %>
      <%= image_tag s.image.url(:thumb) %>
      <hr>
    <% end %>
    <a href="/admin"><button type="button" class="btn btn-success">トップに戻る</button></a>
</div>